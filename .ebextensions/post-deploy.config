commands:
  pre-install:
    command: npm install -g pm2 && npm install -g npx
    ignoreErrors: true
  dependencies:
    command: cd /var/app/current && npm install
  run-app:
    command: cd /var/app/current && pm2 kill && npm run pm2